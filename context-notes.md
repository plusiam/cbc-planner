# 맥락 노트 — 결정과 이유

작업 중 내린 결정을 기록한다. 코드만 보면 다시 도출해야 하는 것들이다. 최신이 위.

> **원칙 (2026-09-04, 사용자)** — 서식과 화면은 **단순하고, 사용자가 직관적으로 이해할 수 있게** 구성한다. 칸을 늘리기 전에 "이 칸이 없으면 설계가 안 되는가"를 묻고, 한 자리에 한 가지 일만 시킨다(스트랜드 자리에 평가를 끌어들이지 않는다).

## 2026-09-04 · 단일교과용도 책의 뼈대로 — 단원 학습의 흐름을 1단계 끝으로

- 「단원 학습의 흐름」을 3단계 맨 위에서 **1단계 끝**으로 옮겼다(책 2장 단원 설계 모델의 1단계 ⑥). 표 id `flow`는 그대로라 저장 데이터가 유지된다.
- 두 서식의 스트랜드 표를 **같은 세 열**로 맞췄다 — 소주제(탐구 목록) · 탐구 질문 (사)/(개)/(논) · 하위 항목(∙ 명사구). 융합교과용에는 하위 항목 열을 더했고, 단일교과용에는 탐구 질문 열을 더했다. 하위 항목은 책의 단원 흐름 예시가 그 열로 되어 있어 남긴 것이다.
- 탐구 질문의 자리는 책(2025, 3단계 ①)과 프로젝트 템플릿(2026-02, 1단계 쪽)이 갈린다. 뒤의 것이 최신 판단이라 스트랜드 표에 같이 둔다.
- 3단계 차시별 계획은 두 서식 모두 손대지 않았다.

## 2026-09-04 · 서식의 토대 — 『초등 수석교사가 풀어내는 깊이 있는 수업 이야기』 2장 (구글 드라이브 근거)

이 웹학습지의 뼈대는 수벤저스가 쓴 책의 **「수석교사가 제안하는 깊이 있는 학습을 위한 단원 설계 모델」**이다. 드라이브 원고 「2장 깊이 있는 학습을 가능하게 하는 교육과정 설계(1장과 합쳐 재구성).pdf」(2025-09-22) 7쪽의 절차를 그대로 옮긴다.

- **1단계 교육과정 분석 및 단원 목표 설정하기** — ① 성취기준 확인 ② 내용 요소 확인 ③ 단원 수준 핵심 아이디어 만들기 ④ 핵심 질문 만들기 ⑤ 개념적 렌즈 정하기 **⑥ 단원 흐름 구성하기**
- **2단계 평가 계획 수립하기** — ① 평가 요소 파악 ② 평가 과제 개발(GRASPS) ③ 평가 기준 마련(잘하고 있어요·발전하고 있어요·힘을 내요) ④ 그 외 평가 계획
- **3단계 학습 경험 계획하기** — ① 학습 주제별 탐구 질문 만들기 ② 탐구 단계를 고려하여 학습 주제별 세부 활동 구성 ③ 사고 전략·자료 준비

책이 말하는 ⑥ 단원 흐름 — "단원의 탐구 목록 또는 학습 주제가 연결된 흐름을 보여준다. 단원 학습 내용을 개괄적으로 한눈에 살펴볼 수 있다." 예시는 학습 주제 4개(이야기 읽기 → 감상 나누기 → 독서 감상문 쓰기 → 발표)와 각 주제의 ∙하위 항목(명사구)으로 되어 있고, **1단계의 마지막 요소**다. 3단계 차시 표는 「차시 · 주요 학습 내용 · 탐구 단계 · 전략□전/자료□자」이며 학습 주제마다 탐구 질문을 머리글로 단다.

같은 결론을 가리키는 다른 자료.
- 「개념기반_탐구_프로젝트_템플릿」(2026-02-24, 구글 문서) — 3 핵심 질문과 탐구 질문(스트랜드별 Ⅰ~Ⅲ) → **4 단원 스트랜드 정하기**(탐구주제 · 하위 핵심아이디어 · 안내질문 · 내용 체계) → 5 평가 → 6 활동 계획(차시 · 스트랜드 · 학습 활동, 관계 맺기 → 스트랜드 1~3 → 일반화 → 전이 → 성찰). 스트랜드가 평가보다 앞이고, 활동 계획은 표 하나.
- 「깊이있는학습_수업설계_통합레퍼런스」(2026-07) — IB PYP 탐구 목록(Lines of Inquiry)은 "중심 아이디어를 여는 3~4개의 탐구 갈래", 설계 흐름은 중심 아이디어 → 탐구 목록+탐구 질문 → 평가 → 탐구 경험 계획. 수벤저스 구성 요소에 「단원 학습 조망(스트랜드 1~6)」.

**적용** — 융합교과용은 오늘 이 뼈대로 맞췄다(탐구 목록 표를 1단계 끝, 3단계는 차시별 계획 하나). 단일교과용은 「단원 학습의 흐름」이 아직 3단계 맨 위에 있어 책의 ⑥과 어긋난다. 탐구 질문의 자리는 책(2025)에서는 3단계 ①, 프로젝트 템플릿(2026-02)에서는 1단계 쪽 — 뒤의 것이 최신 판단이라 표에 같이 둔다.

## 2026-09-04 · 융합교과용 — 탐구 목록(스트랜드)은 1단계 끝, 3단계는 차시별 계획 하나

- 1단계의 「탐구 과제(탐구 목록)」·「탐구 질문」 두 칸이 곧 **단원 흐름도 = 스트랜드를 쓰는 자리**다(사용자). 번호를 일대일로 맞추던 두 칸을 표 하나(`strands`: 탐구 목록 「과제명 (렌즈)」 · 탐구 질문)로 합쳤다. 렌즈 하나에 행 하나. 하위 항목 열은 「단촐하게」라는 요청대로 넣지 않았다 — 필요하면 열 하나 추가.
- 3단계의 「탐구 목록」 카드는 스트랜드 자리에 차시·활동·과정중심 평가·평가 방법까지 일곱 칸을 끌어들이고 있었다. 카드를 없애고, 열기·마무리 표와 합쳐 **차시별 계획 표 하나**(`lessons`: 차시·주제·탐구 단계·학생 주도 탐구 활동)로 만들었다. 단일교과용과 뼈대가 같아졌다.
- 「이 목록에서 볼 것(과정중심)」·「평가 방법」은 뺐다. 과정중심 평가의 시기는 2단계 「그 밖의 평가 증거」의 시기 열이 이미 맡는다. 2단계에 없는 새 평가를 3단계에서 만들지 않는다는 원칙과도 맞다.
- 사라진 키 — f `tasks`·`questions`, g `open`·`close`, c `inquiry`. 서식이 이틀 된 것이라 옛 저장 데이터 이전은 하지 않았다. 불러오면 그 항목만 「서식에 없는 항목」으로 건너뛴다.
- 참조 패널이 표의 열을 읽을 수 있게 `REFS` 키에 `g:표id.열` 형식을 더했다(`g:strands.task`).

## 2026-09-04 · AI 입구 — WebMCP는 절반, 나머지는 붙여넣기

**질문** — Claude·ChatGPT·Gemini·NotebookLM에서 대화하며 짠 설계안이 WebMCP로 cbc-planner에 자동으로 들어가는 구조가 가능한가.

**WebMCP가 맡는 범위** — 웹페이지가 *브라우저 안에 있는 에이전트*에게 도구를 내놓는 규격. 다른 탭·다른 서버의 채팅이 우리 페이지를 원격 조작하는 통로가 아니다. 명세가 목표를 "사람이 함께 있는 로컬 브라우저 워크플로"로, 비목표를 "브라우저 UI 없는 완전 자동화"로 적었다. 그래서 서비스별로 답이 갈린다 (2026-09-04).
- Gemini in Chrome — 가능 예정("soon"). Chrome 149~156 오리진 트라이얼. 한국 2026-04 출시. 자동 브라우징은 미국 구독자 한정.
- ChatGPT 데스크톱 앱 내장 브라우저 — 2026-08-27부터 가능. GPT-5.6 Sol/Terra, Enterprise·Edu 제외. 웹 chatgpt.com은 아님.
- Claude — 없음. Claude in Chrome 확장 요청 #30645 "not planned".
- NotebookLM — 불가. 외부로 행동하지 않는다.

**그래서 택한 구조 — 계획 API 하나 + 입구 셋.** 내부 API `window.cbcPlanner`를 두고, 입구 1(공개 `schema.json` + 프롬프트 팩 + 붙여넣기 + `#plan=` 링크)로 네 서비스 모두 오늘 반자동, 입구 2(WebMCP 도구)로 지원 브라우저에서 자동. 입구 3(원격 MCP 서버)은 서버와 설계안 보관이 생겨 "모든 데이터는 브라우저 안에" 원칙과 어긋나 하지 않는다.

**페이로드 형식은 새로 만들지 않는다.** 작업 파일(.json) 형식 `{app, version, mode, single:{f,g,c,k}, fusion:{…}}`이 정본. 네 입구가 형식 하나를 공유하고 AI가 낸 답이 그대로 유효한 작업 파일이 된다.

**링크로 들어온 설계안은 곧바로 적용하지 않고 붙여넣기 대화상자에 미리 채운다.** 링크 하나로 작업 중인 내용이 덮이는 사고를 막고, 병합/교체 선택과 거절 항목 표시를 붙여넣기와 한 경로로 처리한다.

**`schema-flat.js`를 따로 둔다.** 평탄화 코드를 브라우저(`getSchema`)와 Node(`build-schema.js`)가 같이 써야 `schema.json`이 실제 서식과 어긋나지 않는다. `app.js`는 최상위에서 DOM을 만져 Node에서 읽을 수 없다. `gridInitRows`도 같은 이유로 여기로 옮겼다.

**`build-schema.js`의 vm 함정.** `schema-*.js`의 `const SCHEMA_SINGLE = …`는 `vm.runInNewContext` 뒤 sandbox 속성으로 남지 않는다(let/const는 전역 객체 속성이 아님). 세 파일을 한 스크립트로 이어 붙이고 마지막 식의 값을 받는다.

**부분 setter는 `renderAll`을 부르지 않는다.** `renderAll`은 포커스·펼친 안내를 잃고 연속 호출 시 깜빡인다. input/change 핸들러가 이미 "칸 하나 바뀔 때 할 최소 작업"을 정의하고 있으므로 그대로 흉내 낸다. `applyPlan`·`setMode`만 `renderAll`.

**표 병합은 행 단위가 아니라 표째 교체.** 행은 위치로만 식별되고 `flow`·`lessons`는 init 자리표시 행이 이미 들어 있어 인덱스 겹침이 뜻을 갖지 않는다. 행을 덧붙이는 용도는 `addRow`.

**API는 내용 문제로 throw하지 않고 `rejected` 목록으로 돌려준다.** WebMCP `execute`가 reject되면 에이전트에는 이유 없는 `UnknownError`만 간다. AI가 자기 실수(모르는 키, 서식 뒤바뀜)를 읽을 수 있어야 한다.

**서식 이름 검증은 `=== 'single' || === 'fusion'`.** `SCHEMAS[m]` 검사는 `'constructor'` 같은 프로토타입 속성이 통과한다 (커밋 515333a 교훈).

**WebMCP 네임스페이스는 `document.modelContext`.** W3C 초안 WebIDL·Chrome imperative-api 문서·폴리필 모두 `document`. 초기 기사들이 쓴 `navigator.modelContext`는 예비 감지로만 둔다. 폴리필(22KB)은 번들하지 않는다 — 일반 브라우저에 소비자가 없고 Inspector 확장도 네이티브 플래그를 요구한다.

**프롬프트 팩에 f 키 목록을 인라인으로 넣는다.** 링크를 못 여는 AI(NotebookLM, 브라우징 꺼진 ChatGPT)도 형식은 맞추게. `schema.json`은 작성 규칙(품질)을 더하는 층. 해시 링크 생성을 기본으로 요구하지 않는다 — 코드 실행 없는 모델은 base64를 손으로 만들며 깨뜨린다.

**네 커밋을 한 번에 푸시한다.** 중간 상태가 배포되면 메뉴에 버튼은 있는데 WebMCP는 없는 반쪽이 교실에 나간다.

## 2026-09-04 · 모바일 고정 영역

- 툴바와 단계바를 `.topbar` 한 래퍼로 묶어 sticky. 따로 sticky 시키고 `top: 52px` 상수를 쓰면 툴바가 줄바꿈될 때 단계바가 뒤로 숨는다.
- 고정 영역 높이는 실측해 `--topbar-h`로 넘긴다. 웹폰트 적용으로 높이가 바뀌므로 `document.fonts.ready`에서도 다시 잰다.
- 375px에서 고정 영역 예산은 **약 205px(화면의 25%)**. 이를 지키려고 좁은 화면에서 앱 제목 글자를 감추고 「C」 마크만 남겼다. 문서 제목은 바로 아래 본문에 그대로 나온다.

## 2026-09-04 · 서식에서 (교과) 표기 제거

- 융합교과용의 내용 요소·준거(S)·평가 요소·그 밖의 평가 증거·3단계 「이 목록에서 볼 것」에서 `(교과)` 표기를 뺐다. 프로젝트에 중심 교과와 도구 교과가 있어도 수업자는 개념과 학생의 이해에 초점을 두고 필요한 내용 요소를 선별해 담는다. 평가에서는 더 그렇다 — 준거는 개념과 이해가 수행에서 드러나는지를 보는 자리이지 교과별 성취기준 확인표가 아니다.
- 관련 개념(마이크로 개념)은 **교과별 하위 개념어가 핵심**이라 그대로 둔다. 렌즈를 교과 내용에 붙이는 고리다.
- 목표(G) 안내의 「교과가 여럿이면 교과의 수행이 모두 드러나야」는 잘못된 문구였다. 2단계의 목표는 1단계의 개념과 핵심 아이디어에 초점이 있다.
